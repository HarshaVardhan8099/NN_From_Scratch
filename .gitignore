Python-specific files
These are generated by the Python interpreter and are not part of the source code.
pycache/
*.pyc
*.pyd
*.pyo

IDE-specific files (VS Code)
These files store your local settings and should not be committed to the repository.
.vscode/

Environment files
This is a common name for a Python virtual environment directory.
You should always ignore your virtual environment and only commit a requirements.txt file.
venv/
.venv/

Generated files from your project
The trained model and the generated plot are outputs of your scripts, not source files.
They should not be tracked by Git as they can change and bloat the repository size.
nn_mnist.pkl
test_results.png
devsdevcontainer.json